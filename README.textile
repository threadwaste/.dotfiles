A .dotfiles repository for mobility (and sanity).

Use deploy.sh for a quick and dirty deployment.

bc. git clone https://github.com/threadwaste/.dotfiles ~/.dotfiles
cd ~/.dotfiles
./deploy.sh
git submodule init
git submodule update
vim +BundleInstall +qall
